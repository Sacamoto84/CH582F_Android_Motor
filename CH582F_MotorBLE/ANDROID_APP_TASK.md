# Задание: Android-приложение для настройки помпы

Документ самодостаточный — читать прошивку не обязательно, но полезно.
Прошивка лежит рядом, в этом же репозитории.

---

## 1. Что за устройство

Контроллер помпы для 19-литровой бутыли с водой. Чип **CH582F** (RISC-V,
WCH), плата WeAct CH57x/CH58x Core Board V1.0, питание 1S Li-ion, мотор
0.7 А.

Работает автономно: ручка-потенциометр задаёт мощность, кнопка включает и
выключает. **Приложение нужно только для настройки и наблюдения** — без
телефона устройство полностью функционально.

Что должно уметь приложение:
- найти устройство и подключиться;
- показать живую телеметрию (состояние, скважность, положение ручки,
  напряжение банки);
- прочитать и изменить 12 параметров;
- послать команды: пуск, стоп, сохранить, сброс к заводским, уснуть.

### Про железо — что из этого касается приложения

Отладчика в проекте нет, прошивка заливается по USB через ISP-загрузчик.
Отсюда две вещи, которые видно из приложения:

- **Параметра «окно после сброса» больше нет.** Раньше `BOOT_GRACE_S` (id 11)
  запрещал сон первые секунды после включения, чтобы успел зацепиться
  отладчик. Убран вместе с версией настроек 8. Вместо него в версии 9 появился
  `VBAT_MIN_MV` (id 11) — отсечка по разряду. Параметров по-прежнему 12,
  id 0…11, но одиннадцатый теперь означает совсем другое.
- **Устройство может пропасть из эфира не только уснув.** Заливка прошивки
  начинается со сброса, во время неё связь рвётся так же, как при уходе в сон.
  Отдельно различать эти случаи приложению не нужно: и то и другое — обычный
  разрыв, лечится переподключением.

Свободные ножки контроллера подтянуты в `Board_Init()`, чтобы не висели и не
жгли ток во сне — на протокол и на приложение это никак не влияет, детали в
[HARDWARE.md](HARDWARE.md). Линии USB (PB10/PB11) прошивка не трогает: они
принадлежат ISP-загрузчику.

---

## 2. Как найти устройство

| | |
|---|---|
| Имя в эфире | `MotorCtrl1` |
| UUID сервиса в рекламе | `0xFFE0` (16-битный, `GAP_ADTYPE_16BIT_MORE`) |
| Интервал рекламы | 100 мс |
| Спаривание | без MITM, `NO_INPUT_NO_OUTPUT`, bonding включён |
| Интервал соединения | запрашивает 30…100 мс |
| Supervision timeout | 3 с |

Имя лежит в **scan response**, не в рекламном пакете. Если сканировать без
активного скана, имени не будет — фильтруй по UUID сервиса `0xFFE0`.

Пин-кода нет, диалог сопряжения система покажет сама (Just Works).

**Устройство засыпает.** Через 60 секунд простоя (помпа стоит, никто не
подключён) чип уходит в глубокий сон и пропадает из эфира полностью.
Будится кнопкой на корпусе. Пока телефон подключён — не спит.
Это нормальное поведение, не считать ошибкой связи.

---

## 3. Протокол

Полные 128-битные UUID собираются по базе Bluetooth SIG:
`0000FFE0-0000-1000-8000-00805F9B34FB`.

| Характеристика | UUID | Свойства | Назначение |
|---|---|---|---|
| CMD | `0xFFE1` | Write, Write No Response | команды и запись параметров |
| DATA | `0xFFE2` | Notify | телеметрия, ответы, квитанции |

Обязательно **включить уведомления** на `0xFFE2` — записать `0x0100`
в дескриптор CCCD `0x2902`. Без этого устройство ничего не пришлёт.

Согласовывать MTU **не нужно**: все пакеты не длиннее 20 байт и проходят
при MTU по умолчанию (23). Просить больше не вредно, но бессмысленно.

Весь многобайтовый порядок — **little-endian**.

### 3.1 Запись в CMD — всегда ровно 3 байта

```
[op][value_lo][value_hi]
```

| `op` | Смысл |
|---|---|
| `0x00…0x7F` | записать параметр с этим id, значение — uint16 в `value` |
| `0x80` | прочитать параметр, id в `value_lo`; ответ придёт в notify |
| `0x81` | команда, код в `value_lo` |
| `0x82` | выгрузить все параметры серией notify |

Пакет другой длины устройство отвергнет (`ATT_ERR_INVALID_VALUE_SIZE`).

### 3.2 Пакеты в DATA (notify)

Различаются по первому байту.

**Телеметрия, `type = 0x00`, 10 байт**

| Смещение | Тип | Поле | Смысл |
|---|---|---|---|
| 0 | u8 | `type` | `0x00` |
| 1 | u8 | `state` | 0 стоит, 1 стартовый рывок, 2 работает |
| 2 | u8 | `stop_reason` | 0 не останавливались, 1 пользователь, 2 сработал `MAX_RUN_S`, 3 пуск отклонён отсечкой по разряду |
| 3 | u8 | `pwm_pct` | скважность в процентах, для наглядности |
| 4..5 | u16 | `pwm_pm` | скважность в промилле, точное значение |
| 6..7 | u16 | `pot_raw` | положение ручки, 0…4095 |
| 8..9 | u16 | `vbat_mv` | напряжение банки в милливольтах |

Приходит раз в 200 мс, но только после команды `TELEMETRY_ON`.

**Значение параметра, `type = 0x01`, 4 байта**

| Смещение | Тип | Поле |
|---|---|---|
| 0 | u8 | `type` = `0x01` |
| 1 | u8 | `id` параметра |
| 2..3 | u16 | значение |

Приходит в ответ на `op = 0x80` и серией на `op = 0x82`.

**Квитанция, `type = 0x02`, 3 байта**

| Смещение | Тип | Поле |
|---|---|---|
| 0 | u8 | `type` = `0x02` |
| 1 | u8 | код команды или id параметра |
| 2 | u8 | `status`: 0 принято, 1 отвергнуто |

Приходит на каждую запись параметра и на каждую команду.
`status = 1` означает, что значение не прошло проверку диапазона —
показать пользователю и вернуть прежнее.

---

## 4. Параметры

Все значения — `uint16`. Прошивка проверяет диапазоны и отвечает
квитанцией со `status = 1`, если не прошло.

| id | Имя | Ед. | Диапазон | Дефолт | Что делает |
|---|---|---|---|---|---|
| 0 | `PWM_MIN` | промилле | 0…1000 | 300 | скважность при ручке в минимуме |
| 1 | `PWM_MAX` | промилле | 0…1000 | 950 | скважность при ручке в максимуме |
| 2 | `PWM_FREQ_RUN` | Гц | 919…65535 | 16000 | рабочая частота ШИМ |
| 3 | `PWM_FREQ_BOOST` | Гц | 919…65535 | 1000 | частота на время рывка |
| 4 | `BOOST_EN` | 0/1 | 0…1 | 1 | стартовый рывок включён |
| 5 | `BOOST_POWER` | промилле | 0…1000 | 800 | **абсолютная** скважность рывка |
| 6 | `BOOST_TIME` | мс | 0…5000 | 150 | длительность рывка |
| 7 | `MAX_RUN_S` | с | 0…65535 | 120 | авто-стоп, 0 = без ограничения |
| 8 | `VBAT_SCALE_Q12` | Q12 | 1…65535 | 4666 | калибровка делителя, см. ниже |
| 9 | `VDROP_LEVEL` | код | 0…4 | 4 | сброс по просадке питания |
| 10 | `SLEEP_TOUT_S` | с | 0…65535 | 60 | простой до ухода в сон, 0 = не спать |
| 11 | `VBAT_MIN_MV` | мВ | 0…4200 | 3000 | ниже порога пуск запрещён, 0 = защиты нет |

Промилле: 1000 = 100 %. В интерфейсе показывать процентами с одним знаком
или целыми процентами — на усмотрение.

`VDROP_LEVEL`: 0 выключено, 1 = 1.9 В, 2 = 2.1 В, 3 = 2.3 В, 4 = 2.5 В.
Порог просадки VDD33, при котором чип делает аварийный сброс.

`PWM_MIN` и `PWM_MAX` прошивка сама меняет местами, если их перепутать.
После записи стоит перечитать оба.

Нижняя граница частоты 919 Гц — аппаратное ограничение делителя ШИМ,
меньше чип не умеет.

### Калибровка `VBAT_SCALE_Q12`

Напряжение банки считается как `vbat_mv = ADC * scale / 4096`. Заводское
значение приблизительное. Порядок:

1. измерить банку мультиметром;
2. посмотреть `vbat_mv` в телеметрии;
3. записать `new = old * реальные_мВ / показанные_мВ`.

Хорошо бы сделать это мастером в приложении: поле «реальное напряжение,
мВ» и кнопка «откалибровать», пересчёт внутри.

---

## 5. Команды (`op = 0x81`, код в `value_lo`)

| Код | Команда | Что делает |
|---|---|---|
| 0 | `MOTOR_STOP` | остановить помпу |
| 1 | `MOTOR_START` | запустить помпу; `status = 1`, если банка ниже `VBAT_MIN_MV` |
| 2 | `SAVE` | записать настройки в DataFlash **немедленно** |
| 3 | `FACTORY_RESET` | вернуть заводские значения |
| 4 | `SLEEP` | остановить помпу и уйти в глубокий сон |
| 5 | `TELEMETRY_ON` | начать слать телеметрию раз в 200 мс |
| 6 | `TELEMETRY_OFF` | прекратить |

### Как устроено сохранение

Запись параметра применяется **сразу**, но только в ОЗУ. Во flash значение
попадает исключительно по команде `SAVE` — автоматической записи нет.
Пока пользователь не нажал «Сохранить», изменения переживут разрыв связи,
но не сброс и не уход в сон.

Приложение обязано показывать, что есть несохранённые изменения, и
переспрашивать перед отключением и перед командой `SLEEP`.

`SAVE` на работающей помпе отвечает `status = 1` и ничего не пишет: стирание
страницы блокирует ядро на десятки миллисекунд. Сначала стоп, потом
сохранение.

`FACTORY_RESET` сохраняется сам — сброс к заводским подразумевает запись.

Ресурс flash 100 000 циклов, поэтому **не слать запись на каждое движение
ползунка**. Дебаунс 300–500 мс после того, как пользователь отпустил
ползунок, либо запись по кнопке «Применить».

`SLEEP` разрывает соединение — это ожидаемо, не показывать как ошибку.

---

## 6. Сценарии, которые надо покрыть

**Подключение.** Сканирование по UUID `0xFFE0` → подключение → включить
notify на `0xFFE2` → прочитать параметры → заполнить форму → послать
`TELEMETRY_ON`.

Параметры можно взять одним `op = 0x82` (серия из 12 пакетов, ~120 мс) либо
поштучно через `op = 0x80`, дожидаясь ответа на каждый. Второй способ дольше
(12 обменов по интервалу соединения), зато сам себя проверяет: видно, какой
именно параметр не дошёл, и его можно перезапросить. Приложение читает
поштучно.

Телеметрию включать **после** чтения параметров: пока она молчит, у стека
свободны буферы уведомлений.

**Экран наблюдения.** Состояние помпы, скважность, положение ручки,
напряжение банки. Кнопки «Пуск» и «Стоп». Обновление 5 раз в секунду.

**Экран настроек.** 12 полей. У числовых — ползунки с числовым вводом,
у `BOOST_EN` — переключатель, у `VDROP_LEVEL` — выпадающий список.
Показывать заводское значение и подсказку, что параметр делает.
`VBAT_MIN_MV` вводить и показывать в вольтах — мультиметр меряет в них,
и человеку понятнее «3.00 В», чем «3000».

**Отказ пуска по разряду.** Если банка ниже `VBAT_MIN_MV`, помпа не
запустится: на `MOTOR_START` придёт квитанция со `status = 1`, устройство
пискнет сигналом ошибки, а в телеметрии `stop_reason` станет 3. Показать
это внятно — пользователь должен понять, что надо зарядить банку, а не
чинить помпу. Отказ приходит и на нажатие кнопки на корпусе, там квитанции
нет — только `stop_reason` в телеметрии.

**Мастер подбора рывка.** Самая полезная функция, ради неё всё и затевалось:
1. `BOOST_EN = 0`, ручку в минимум → поднимать `PWM_MIN`, пока помпа не
   начнёт уверенно держать вращение;
2. `BOOST_EN = 1` → поднимать `BOOST_POWER`, пока не начнёт уверенно
   трогаться с ручки в минимуме;
3. `BOOST_TIME` — минимальное, при котором успевает раскрутиться,
   обычно 100–200 мс.

Приложение может вести пользователя по шагам с кнопками «пробный пуск».

**Обрыв связи.** Устройство может уснуть или уйти из зоны. Показывать
состояние соединения и предлагать переподключиться, а не падать.

---

## 7. Технические решения — на усмотрение новой сессии

Ничего не зафиксировано. Разумный набор: Kotlin, минимальный SDK 26+,
Jetpack Compose, корутины. Для BLE — либо голый `BluetoothGatt`, либо
обёртка вроде Nordic Android-BLE-Library (она снимает боль с очередью
операций GATT).

Что не забыть в Android:
- разрешения `BLUETOOTH_SCAN` и `BLUETOOTH_CONNECT` для API 31+,
  `ACCESS_FINE_LOCATION` для более старых;
- операции GATT нельзя пускать параллельно — только по очереди;
- колбэки GATT приходят не в главном потоке.

---

## 8. Где посмотреть исходники прошивки

| Что | Файл |
|---|---|
| Протокол: id, коды, структуры пакетов | `APP/include/app_proto.h` |
| GATT-сервис, разбор команд | `Profile/motor_service.c` |
| Диапазоны и дефолты параметров | `APP/settings.c` |
| Реклама, имя, параметры соединения | `APP/peripheral.c` |
| Логика помпы, откуда берётся телеметрия | `APP/motor_task.c` |
| Общее описание проекта | `README.md` |
| Схема и распиновка | `HARDWARE.md` |

`app_proto.h` — источник истины. Если этот документ ему противоречит,
прав заголовок.

---

## 9. Сначала проверить руками

До написания кода стоит убедиться, что железо отвечает: **nRF Connect**
на телефоне, подключиться к `MotorCtrl1`, включить уведомления на `0xFFE2`,
записать в `0xFFE1`:

| Байты | Что произойдёт |
|---|---|
| `82 00 00` | придут 12 пакетов `01 <id> <lo> <hi>` со всеми параметрами — по одному за тик, вся серия примерно за 120 мс |
| `81 05 00` | пойдёт телеметрия раз в 200 мс |
| `81 01 00` | помпа запустится |
| `81 00 00` | помпа остановится |
| `80 05 00` | придёт `01 05 20 03` — `BOOST_POWER` = 800 |
| `05 2C 01` | `BOOST_POWER` = 300, придёт квитанция `02 05 00` |

Если это работает — протокол живой и можно писать приложение.
